name: animated_transitions
description: "A collection of beautiful transitions to navigate between pages in a Flutter app."
version: 1.0.6
homepage: https://www.baansoftware.com
repository: https://github.com/baan-software/flutter-animated-transitions

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
