name: animated_transitions_example
description: "Demonstrates how to use the animated_transitions package."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  animated_transitions:
    path: ../

  cupertino_icons: ^1.0.8
  go_router: ^16.0.0
  auto_route: ^10.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true 